           ------GIT Commands------
git clone https://github.com/NTharu/Snowflake_test.git
git checkout snowflake_test  [--branch name]
git status
git add -all (or) git add .
git commit -m "message here"
git push origin snowflake_test
           ------merging branches------
if we want to merge this branch with main then
git checkout main
git merge snowflake_test